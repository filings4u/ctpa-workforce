(()=>{'use strict';
const $=(s,r=document)=>r.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),pretty=v=>String(v??'—').replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase());
const badge=v=>`<span class="badge ${/active|complete/i.test(String(v))?'good':/inactive|terminated/i.test(String(v))?'bad':'warn'}">${esc(pretty(v))}</span>`;
function render(d){const rows=(d.employees||[]).filter(x=>String(x.workforce_worker_type||'')==='driver');return `<div class="panel"><div class="panel-head"><div><h2>NON-DOT Drivers</h2><p>Driver-designated workers managed through this NON-DOT Workforce C/TPA account.</p></div><span class="badge">${rows.length} driver${rows.length===1?'':'s'}</span></div><div class="table-wrap"><table><thead><tr><th>Driver</th><th>Employer</th><th>Employee #</th><th>Position</th><th>Safety-Sensitive</th><th>Status</th></tr></thead><tbody>${rows.length?rows.map(x=>`<tr><td><strong>${esc([x.first_name,x.last_name].filter(Boolean).join(' ')||'Driver')}</strong><small>${esc(x.email||'')}</small></td><td>${esc((d.employers||[]).find(e=>e.id===x.employer_id)?.legal_name||'—')}</td><td>${esc(x.employee_number||'—')}</td><td>${esc(x.job_title||'—')}</td><td>${badge(x.safety_sensitive?'yes':'no')}</td><td>${badge(x.employment_status||'active')}</td></tr>`).join(''):'<tr><td colspan="6"><div class="empty">No NON-DOT Driver records are available.</div></td></tr>'}</tbody></table></div></div>`}
window.CtpaOwnerOperators={render,bind(){}};
})();
